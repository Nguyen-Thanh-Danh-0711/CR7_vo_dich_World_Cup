server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/shopcloud_auth
    username: shopcloud_admin
    password: admin_password_123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

jwt:
  secret: ShopCloudAuthServiceSecretKey256BitMinimumLengthRequired!!
  expiration-ms: 86400000
